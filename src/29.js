function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
}

// Example usage: 
const randomNumber = getRandomInt(10);
console.log(randomNumber); // Output will depend on the maximum value passed to getRandomInt()
